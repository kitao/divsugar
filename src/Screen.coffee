DivSugar._Screen =
  _initialize: ->
    #

  getSize: ->
    #

  setSize: (outerW, outerH, innerW, innerH) ->
    #

  getPerspective: ->
    #

  setPerspective: ->

  # getBackgroundColor/setBackgroundColor?
